import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cartao-de-identificacao',
  webDir: 'www'
};

export default config;
